global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: three-zero-four-game-service
    static_configs:
      - targets:
          - game-service:4100
